# 桌面版（Tauri）

## 应用位置

可运行的 Tauri 工程在 **[`ilink-hub-desktop/`](ilink-hub-desktop/)** 目录（`npm create tauri-app` 脚手架 + 已接入 `ilink_hub::run_serve`）。

请先阅读该目录下的 [`ilink-hub-desktop/README.md`](ilink-hub-desktop/README.md)（开发命令、环境变量、数据库路径说明）。

## 路线图

整体规划见 [docs/desktop-tauri-roadmap.md](../docs/desktop-tauri-roadmap.md)。
