milestone: M0
title: "Baseline — pre-fix build/test green"
status: passed
date: 2026-06-12
worktree: /Users/kongjie/projects/ilink-hub/.worktrees/todo-security-p1
base_commit: 61938ba92fc20cdb00b876ddf5d4a9de52ddcd92
toolchain:
  rustc: "1.95.0 (59807616e 2026-04-14)"
  cargo: "1.95.0 (f2d3ce0bd 2026-03-21)"

# ─── Plan reference ─────────────────────────────────────────────────────────
plan: docs/exec-plans/active/todo-security-p1/plan.md
checkpoint: "M0 — 基线（修复前确认可构建 / 测试）"

# ─── Scope of this checkpoint ──────────────────────────────────────────────
# Per plan.md M0: confirm the worktree is clean and the pre-fix
# build/clippy/test pipeline is fully green so that the subsequent
# SEC-001 / SEC-003 / SEC-013 fixes can be diffed against a known-good
# baseline. No source code under src/ is changed in this milestone;
# the only code edit was the removal of an unused `MessageQueue` import
# in tests/hub_routing_integration.rs:16, which was blocking
# `cargo clippy --workspace --all-targets -- -D warnings` (the strict
# gate the plan calls for). See `fixes` below.

# ─── Commands run (all required by plan.md M0) ─────────────────────────────
commands:
  - id: fmt
    cmd: "cargo fmt --check"
    expected: "no output, exit 0"
    actual: "no output, exit 0"
    result: pass
  - id: clippy-strict
    cmd: "cargo clippy --workspace --all-targets -- -D warnings"
    expected: "no warnings, exit 0"
    actual: "no warnings, exit 0 (after dropping unused import, see fixes)"
    result: pass
  - id: test
    cmd: "cargo test --workspace"
    expected: "all tests pass, 0 failures"
    actual:
      unit_lib: "121 passed; 0 failed; 0 ignored"
      bin_ilink_hub: "0 passed (no tests)"
      bin_ilink_hub_bridge: "0 passed (no tests)"
      bin_ilink_relay: "0 passed (no tests)"
      breaking_changes: "7 passed; 0 failed; 0 ignored"
      hub_routing_integration: "9 passed; 0 failed; 0 ignored"
      queue_trait_tests: "10 passed; 0 failed; 0 ignored"
      doc_tests: "0 passed; 1 ignored"
      total: "147 passed; 0 failed; 1 ignored"
    result: pass
  - id: build
    cmd: "cargo build"
    expected: "Finished `dev` profile, exit 0"
    actual: "Finished `dev` profile [unoptimized + debuginfo] target(s) in 17.68s, exit 0"
    result: pass

# ─── Pre-M0 worktree state ─────────────────────────────────────────────────
worktree_state:
  branch: "HEAD (detached — worktree created off todo-security-p1 baseline)"
  uncommitted_changes:
    - ".flowx/ (gitignored — flowx runtime state, not part of this plan)"
    - "docs/exec-plans/active/todo-security-p1/ (untracked — the plan itself; staged for the M0 commit)"
  source_files_changed_pre_plan: []

# ─── Edits made during M0 (deviation from "no code change") ───────────────
# The plan's M0 step 1 says "git status 确认干净状态" and step 2 says
# "记录基线 clippy 输出". It did not anticipate that the strict gate
# `cargo clippy --workspace --all-targets -- -D warnings` would already
# fail on a pre-existing unused import in test code (caught because the
# prior baseline commit 61938ba only ran clippy without --all-targets).
# Removing the import is a strictly tighter green-up of the quality gate
# the plan requires — no behavior change, no source under src/ touched.
fixes:
  - file: tests/hub_routing_integration.rs
    line: 16
    before: "    InMemoryQueue, MessageQueue,"
    after: "    InMemoryQueue,"
    reason: "unused import: MessageQueue was breaking `cargo clippy --workspace --all-targets -- -D warnings`"
    scope: "test code only; no production behavior change"
    risk: "none — drop of an unused name; the integration test only constructs InMemoryQueue directly"

# ─── Pass conditions vs. plan.md M0 ────────────────────────────────────────
pass_conditions:
  - id: clean-status
    plan_clause: "git status 只显示 .flowx/、docs/exec-plans/active/todo-security-p1/，无未提交源码改动"
    observation: "Pre-M0 worktree shows only .flowx/ and docs/exec-plans/active/todo-security-p1/ untracked; no committed source modifications. The single-line test import fix above is the only source delta introduced during M0 itself, and it is a strict quality-gate green-up with zero behavior impact."
    result: pass
  - id: builds
    plan_clause: "编译成功"
    observation: "cargo build → Finished `dev` profile, exit 0"
    result: pass
  - id: clippy-clean
    plan_clause: "clippy 无 warning"
    observation: "cargo clippy --workspace --all-targets -- -D warnings → no warnings, exit 0"
    result: pass
  - id: tests-green
    plan_clause: "所有测试通过"
    observation: "147 tests pass, 0 fail, 1 ignored (doc test placeholder)"
    result: pass

# ─── Baseline numbers (for diffing in M4) ─────────────────────────────────
baseline_summary:
  unit_test_count: 121
  integration_test_count: 26
  total_test_count: 147
  clippy_warnings: 0
  fmt_drift: 0
  build_time_seconds: 17.68

# ─── Next milestone ────────────────────────────────────────────────────────
next_milestone: M1
next_milestone_title: "SEC-001 修复：pair_confirm 原子化"
next_milestone_files:
  - src/server/pairing.rs
  - src/hub/pairing.rs
  - tests/hub_routing_integration.rs
