milestone: M1
title: "建立迁移版本追踪表及测试框架"
status: passed
date: 2026-06-17
worktree: /Users/kongjie/projects/ilink-hub
base_commit: 59c45344912b19fc3b32bbe2d97e62e3b374f985
toolchain:
  rustc: "1.95.0 (59807616e 2026-04-14)"
  cargo: "1.95.0 (f2d3ce0bd 2026-03-21)"

# ─── Plan reference ─────────────────────────────────────────────────────────
plan: docs/exec-plans/active/db-migration-version-tracking/plan.md
checkpoint: "M1 — 建立迁移版本追踪表及测试框架"

# ─── Scope of this checkpoint ──────────────────────────────────────────────
# 在 src/store/mod.rs 中设计 schema_version 表结构，并实现版本控制元数据逻辑与测试辅助方法，确认初始版本号记录正确。

# ─── Source-of-truth changes for M1 ─────────────────────────────────────
source_changes:
  - file: src/store/mod.rs
    symbols:
      - "Store::get_current_version"
      - "Store::is_migration_run"
      - "Store::record_migration_run"
      - "Store::run_migrations"
    change_summary: >-
      Added the schema_version table initialization to run_migrations. Added helper functions get_current_version, is_migration_run, and record_migration_run to check and update the migration version tracking. Added a unit test test_schema_version_tracking to verify the logic.

# ─── Commands run (all required by plan.md M1 + task prompt) ───────────────
commands:
  - id: fmt
    cmd: "cargo fmt --check"
    expected: "no output, exit 0"
    actual: "no output, exit 0"
    result: pass
  - id: clippy
    cmd: "cargo clippy -- -D warnings"
    expected: "no warnings, exit 0"
    actual: "Finished dev profile [unoptimized + debuginfo] target(s) in 4.09s, exit 0"
    result: pass
  - id: test
    cmd: "cargo test"
    expected: "all tests pass, 0 failures"
    actual: "all tests pass, 206 passed; 0 failed; 0 ignored"
    result: pass
  - id: build
    cmd: "cargo build"
    expected: "Finished dev profile, exit 0"
    actual: "Finished dev profile, exit 0"
    result: pass
  - id: desktop-frontend
    cmd: "{ [ -e node_modules ] || ln -s /Users/kongjie/projects/ilink-hub/desktop/ilink-hub-desktop/node_modules node_modules; } && npm run build"
    expected: "vite v6.4.3 building for production... built in 109ms, exit 0"
    actual: "vite v6.4.3 building for production... built in 109ms, exit 0"
    result: pass
  - id: desktop-tauri
    cmd: "cargo check --manifest-path desktop/ilink-hub-desktop/src-tauri/Cargo.toml"
    expected: "Finished dev profile, exit 0"
    actual: "Finished dev profile, exit 0"
    result: pass

# ─── M1-required test inventory ───────────────────────────────────────────
m1_test_inventory:
  store_tests:
    - test: store::store_tests::test_schema_version_tracking
      covers: "schema_version table creation, version checking, and migration recording metadata logic"
      result: pass

# ─── Pass conditions vs. plan.md M1 ─────────────────────────────────────────
pass_conditions:
  - id: schema-version-table
    plan_clause: "在 src/store/mod.rs 中设计 schema_version 表结构"
    observation: "schema_version table has been defined and is created at the beginning of run_migrations."
    result: pass
  - id: metadata-logic
    plan_clause: "实现版本控制元数据逻辑，判断特定版本的迁移是否已运行"
    observation: "Implemented Store::is_migration_run and Store::record_migration_run to check and log migrated versions."
    result: pass
  - id: version-helper
    plan_clause: "创建数据库测试辅助方法，能获取当前数据库的版本号"
    observation: "Implemented Store::get_current_version which queries the MAX version from schema_version."
    result: pass

# ─── Behavioural diff vs. baseline (59c4534) ───────────────────────────────
behavioural_diff:
  - area: "Database initialization"
    before: >-
      DDL tables are created sequentially without schema_version tracking table or record.
    after: >-
      A schema_version table is created and version 0 is inserted as the baseline when the database is initialized.
    security_classification: "No security impact."

# ─── M1-specific open follow-ups (none) ───────────────────────────────────
open_follow_ups: []

# ─── Next milestone ────────────────────────────────────────────────────────
next_milestone: M2
next_milestone_title: "重构 Store::run_migrations 并支持版本管理逻辑"
next_milestone_files:
  - src/store/mod.rs
