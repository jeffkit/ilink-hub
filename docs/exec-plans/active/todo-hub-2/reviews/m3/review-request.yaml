milestone: M3
title: "Fix [S-01] vtoken exposure in debug logs"
status: passed
date: 2026-06-12
worktree: /Users/kongjie/projects/ilink-hub/.worktrees/todo-hub-2
base_commit: efa6a45918b61c42a1aaee0bf16b8aea327e4f9f
toolchain:
  rustc: "1.95.0 (59807616e 2026-04-14)"
  cargo: "1.95.0 (f2d3ce0bd 2026-03-21)"

# ─── Plan reference ─────────────────────────────────────────────────────────
plan: docs/exec-plans/active/todo-hub-2/plan.md
checkpoint: "M3 — Fix [S-01] vtoken exposure in debug logs"

# ─── Scope of this checkpoint ──────────────────────────────────────────────
# Redact `vtoken` in `src/hub/router.rs:159` to print only the first 8 characters.

# ─── Source-of-truth changes for S-01 ─────────────────────────────────────
source_changes:
  - file: src/hub/router.rs
    symbols:
      - "Router::route"
    change_summary: >-
      Modified the debug log in the routing path to print only the first 8 characters
      of the `vtoken` using slice indexing. Added a corresponding unit test to verify log redaction.

# ─── Commands run (all required by plan.md M3 + task prompt) ───────────────
commands:
  - id: fmt
    cmd: "cargo fmt --check"
    expected: "no output, exit 0"
    actual: "no output, exit 0"
    result: pass
  - id: clippy
    cmd: "cargo clippy -- -D warnings"
    expected: "no warnings, exit 0"
    actual: "no warnings, exit 0 (Finished `dev` profile, no diagnostics)"
    result: pass
  - id: test
    cmd: "cargo test"
    expected: "all tests pass, 0 failures"
    actual: "all tests pass, 124 passed for lib, 7 for breaking changes, 9 for hub routing, 10 for queue trait"
    result: pass
  - id: build
    cmd: "cargo build"
    expected: "Finished `dev` profile, exit 0"
    actual: "Finished `dev` profile, exit 0"
    result: pass

# ─── M3-required test inventory (plan §M3) ─────────────────────────────────
m3_test_inventory:
  unit_router:
    - test: hub::router::tests::route_redacts_vtoken_in_logs
      covers: "Routing decisions and logs redact the vtoken to 8 characters"
      result: pass

# ─── Pass conditions vs. plan.md M3 ─────────────────────────────────────────
pass_conditions:
  - id: redact-vtoken
    plan_clause: "Redact vtoken in src/hub/router.rs:159 to print only the first 8 characters (e.g., %&vtoken[..vtoken.len().min(8)])"
    observation: "Modified debug log to redact vtoken to `&vtoken[..vtoken.len().min(8)]`."
    result: pass
  - id: verification-passed
    plan_clause: "Verification Command: cargo test --lib hub::router::tests"
    observation: "All unit tests in hub::router::tests compiled and ran cleanly, including the new route_redacts_vtoken_in_logs test."
    result: pass

# ─── Behavioural diff vs. baseline (efa6a45) ───────────────────────────────
behavioural_diff:
  - area: "Vtoken Logging in Router"
    before: >-
      The full `vtoken` credential was logged in plain text when a message was successfully routed to a backend client.
    after: >-
      The debug log only contains the first 8 characters of the `vtoken`, ensuring credentials are not exposed in debug logs.
    security_classification: "Fixes a security exposure (S-01)."

# ─── M3-specific open follow-ups (none) ───────────────────────────────────
open_follow_ups: []

# ─── Next milestone ────────────────────────────────────────────────────────
next_milestone: M4
next_milestone_title: "Fix [C-01] Broadcast persist fire-and-forget window"
next_milestone_files:
  - src/hub/mod.rs
