# Knowledge Bundle 更新日志

## 2026-06-18

* **Creation**: 初始化 OKF knowledge bundle，从 `AGENTS.md` 拆分并结构化。
* **Creation**: 新增 `project/`（overview、quality-gates、conventions）。
* **Creation**: 新增 `bridges/`（overview、profile-protocol、cursor、claude-code）。
* **Creation**: 新增 `api/`（commands、configuration）。
* **Creation**: 新增 `dev-workflow/`（force-dev、common-commands）。
