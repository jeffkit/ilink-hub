# 已废弃：单文件多 profile + routing 已移除。
#
# 请改为：
#   1. 每个后端一个 YAML（本目录其它 example）
#   2. 用 `ilink-hub-bridge manager` 加载 ~/.ilink-hub-bridge/profiles/*.yaml
#   3. 微信里用 Hub `/use <name>` 切换
#
# 迁移对照见 docs/knowledge/bridges/profile-protocol.md「从旧 YAML 迁移」

description: REPLACE_ME — use one executor per file
agentproc:
  executor: claude-code
  cwd: /path/to/your/project
