# 内置 executor: claude-code
description: Claude Code on project
agentproc:
  executor: claude-code
  cwd: /path/to/your/project
  timeout_secs: 600
  max_reply_chars: 8000
  streaming: true
  send_error_reply: true
  # env:
  #   CLAUDE_MODEL: sonnet
  #   ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
