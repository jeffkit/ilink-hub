# 自定义 SDK handler（Python / Node）
description: custom agentproc SDK handler
script: /path/to/handler.py
agentproc:
  timeout_secs: 60
  max_reply_chars: 8000
  streaming: true
  send_error_reply: true
  env:
    MY_API_KEY: ${MY_API_KEY}
