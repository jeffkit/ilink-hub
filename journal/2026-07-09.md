# 2026-07-09

### 15:00 — fix: security-hardening-abc（A+B+C）

- 架构审计后按用户确认推进：A Critical、B High（保留 dangerous flag）、C=3 god 拆分
- M1：CORS 接到 `build_router`；配对 vtoken 120s claim-window
- M2：shell+`-c`+`{{MESSAGE}}` 硬拒绝；DATABASE_URL 脱敏；桌面强制 loopback（修兜底回读）
- M3：`dispatcher` 拆子模块；desktop `lib.rs` 抽出 listen_addr/hub_commands/bridge_profiles
- M4：知识库对齐默认 loopback / 队列易失；归档过期 active plans
- 分支：`fix/security-hardening-abc`
- 关联：`docs/exec-plans/completed/security-hardening-abc/`

### 17:50 — chore: 修复变异测试基建（examine/exclude 冲突）

- 修复 `.cargo/mutants.toml`：Phase H–K 写入 examine 的 bridge/mcp/server/crypto 被 exclude 整树挡掉（18 文件空转）
- 刷新扫描：login 0%→90.9%、crypto 100%、paths 33%→100%、commands 75%→97.4%、dispatch 63%→77.3%
- store 初扫：sessions 85.3%、messages 95.7%、context 72.7%、clients 40%（下一优先）
- 补测：login/crypto/paths；commands 空 ctx + session_list；dispatch ForwardTo 空 ctx；良性 missed 写入 exclude_re
- 文档：README/baseline/knowledge 索引/status 同步；gitignore 增加 mutants.out*
- 分支：`chore/mutation-testing-infra-fix`（PR #20）
- 关联：`docs/mutation-testing/`、`docs/exec-plans/active/mutation-test-coverage-p2/`
