### 12:26 — test: store/clients mutation score 40% → 100%

- 补 6 个 store_tests：touch_client、get_client_by_name、update_client_description、delete_client_by_name、update_client_by_vtoken、update_client_persona
- 重扫：`cargo mutants --no-config -j 2 --file src/store/clients.rs` → 22/22 caught（2 unviable）
- 更新 baseline / status
- 分支：`chore/mutation-testing-infra-fix`（PR #20）

### 15:55 — merge PR #20 + dispatch mutation catch-up

- 合并 PR #20（mutation infra + store 补测）到 main
- 抽 `handle_broadcast`（Router 已不再产出 Broadcast，需白盒测 fan-out）
- 补测：@mention 空 ctx、quote 空 vtoken、broadcast push_shared、footer `session-` vs `||`→`&&`
- 重扫 dispatch：44 caught / 3 missed（均为 `resp.ret` 日志分支，已 exclude_re）→ **93.6% → ~100%***
- 分支：`test/dispatch-mutation-catch-up`

### 18:00 — fix: arch P0 hardening（锁/shutdown/归属/fail-closed）

- 修 `handle_cmd_list` AB-BA：先 clone clients 再取 router
- 修 `load_clients_from_db` / `register_client_in_hub`：释放 router 锁后再 await DB；路由加载不再同时持两锁
- dispatcher + upstream polling：`select!` 响应 shutdown；backoff sleep 可中断
- `resolve_send_context` / `getconfig`：校验 vtoken 对 vctx 归属；dispatch 在 push 前同步 grant
- `warn_if_insecure`：公网绑定 + insecure 拒绝启动
- 分支：`fix/arch-p0-hardening`（worktree）

### 18:30 — fix: arch P1/P2 加固续作

- 配对：`get_bot_qrcode` 按 IP 限流；confirmed 后 `bot_token` 走 claim-window（PR#19 保留，优于纯 one-shot）
- AdminGuard 统一 register/delete/update/ilink_status/metrics（qr-stream 仍支持 ticket）
- CORS：`parse_origins`/`build_cors_layer` 改 Result；非法白名单不再 panic 成 permissive
- relay：非 loopback 的 `http://` 自动升 `wss://`
- executor：stderr JoinError 打 warn
- 文档：master key 轮转 SOP + 配对/CORS 加固说明

### 19:40 — refactor: 拆分 routes + hub/dispatch（基于当前 main）

- `server/routes.rs` → `auth` / `bot` / `admin` / `metrics` / `wait`
- `hub/dispatch.rs` → `pipeline` / `quote` / `mention` / `queue` / `hub_ext`
- `bridge/dispatcher` 已在 main（PR#19）拆过，本轮不动
- 与 worktree `refactor/split-large-modules`（基于 e5b88ea）分叉，改为在 origin/main 上重拆以避免冲突
- 分支：`refactor/split-routes-dispatch`

### 19:45 — fix: 将 arch-p0 加固合入当前 main（解决与 PR#19 冲突）

- cherry-pick e5b88ea 到拆分后的模块路径；删除复活的 routes.rs/dispatch.rs
- 配对：保留 PR#19 的 `claim_confirmed_vtoken` claim-window（优于纯 one-shot）
- 移植：dispatcher shutdown select、同步 grant vctx、AdminGuard 统一、sendmessage/getconfig 归属校验
- 分支：`fix/arch-p0-onto-main`
