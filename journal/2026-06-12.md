# Journal — 2026-06-12

## feature: todo-runtime

**Status:** ✅ Completed (CHAN-02 and SYNC-01 resolved; exec-plan archived)
**Branch:** `feat/todo-runtime`
**Worktree:** `.worktrees/todo-runtime`

---

## 摘要

完成 `todo-runtime` Runtime 模块修复批次，涵盖 CHAN-02 (广播通道容量扩容与延迟监控) 与 SYNC-01 (启动恢复路由时过滤无效 vtoken) 两项可靠性与一致性优化，质量门限 (clippy / test / fmt) 全绿。

## 关键里程碑

| Milestone | 范围 | 状态 |
|-----------|------|------|
| CHAN-02 | 广播通道容量从 256 扩容至 1024，并添加 lagged 丢包 metrics 可观测性 | ✅ certified |
| SYNC-01 | `load_clients_from_db` 恢复路由时，过滤掉 registry 中不存在的 vtoken 路由 | ✅ certified |
| 收尾 | exec-plan 归档 | ✅ done |

## 代码变更概览

- **src/runtime/** — `serve.rs` (+7 / -2) (广播通道容量调优至 1024，并在加载路由时读取 registry 校验 vtoken 存在性)
- **src/hub/** — `mod.rs` (+4 / -1) (在 Metrics 结构体中引入 `dispatcher_lagged` 计数器，并在 spawn_dispatcher lagged 错误时进行累加)
- **src/server/** — `routes.rs` (+8 / -0) (在 `/metrics` 接口输出中注入 `ilink_hub_dispatcher_lagged_total` 指标)
- **tests/** — `hub_routing_integration.rs` (+1 / -2) (清理 unused import `MessageQueue` 以保证 quality-gate 绿灯)

**合计:** 4 文件, +23 / -3 行 (不含 exec-plan / flowx 配置文件)

## 修复的核心可靠性/安全问题

- **CHAN-02** 广播通道慢处理静默丢包问题 — 将广播通道容量提升至 1024 增加高峰缓冲，并通过在 `spawn_dispatcher` 中截获 `Lagged` 错误将其累加至 `dispatcher_lagged` 指标暴露到 `/metrics`，实现消息丢失的完全可观测性。
- **SYNC-01** 启动恢复路由包含失效 vtoken 问题 — 在 `load_clients_from_db` 加载历史路由时，先对 vtoken 进行 registry 存在性检查，确保不会将死路由装载进内存中从而导致后续消息被静默丢弃。

## 验证

- `cargo fmt --check` — green
- `cargo clippy --all-targets -- -D warnings` — green
- `cargo test` — green
- `cargo build` — green

## 提交记录 (2 commits)

```
8b0f01c fix(runtime): increase broadcast capacity (CHAN-02) and filter invalid vtoken routes (SYNC-01)
8be93d8 docs: archive exec-plan for todo-runtime
```

## 遗留 / 后续

- 无未关闭的 Runtime 模块可靠性或安全风险
- 准备合并到 `main` 分支前的最终冲突复核
