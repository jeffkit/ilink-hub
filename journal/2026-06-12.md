# Journal — 2026-06-12

## feature: todo-store

**Status:** ✅ Completed (SYNC-02, DB-03, DB-02 resolved; exec-plan archived)
**Branch:** `feat/todo-store`
**Worktree:** `.worktrees/todo-store`

---

## 摘要

完成 `todo-store` Store 模块修复批次，涵盖 SYNC-02 (客户端 upsert 时同步更新活跃路由 vtoken 状态)、DB-03 (使用 OR 链替换 IN 元组查询以兼容不同数据库) 与 DB-02 (批量持久化 context token 分批事务提交以避免数据库写锁阻塞) 三项性能、兼容性与一致性优化，质量门限 (clippy / test / fmt) 全绿。

## 关键里程碑

| Milestone | 范围 | 状态 |
|-----------|------|------|
| SYNC-02 | `upsert_client` 事务中，当客户端 vtoken 变化时同步更新 `routing_state` 中的 vtoken，防止路由失效 | ✅ certified |
| DB-03 | `get_hub_ext_batch` 使用 `OR` 链查询替换元组 `IN` 语法，兼容 MySQL 5.7/SQLite/PostgreSQL | ✅ certified |
| DB-02 | `persist_context_tokens_batch` 限制单次事务持久化 batch 为 50，以缓解大批量广播时的写锁竞争 | ✅ certified |
| 收尾 | exec-plan 归档 & 修复 clippy 警告 | ✅ done |

## 代码变更概览

- **src/store/** — `mod.rs` (+237 / -27) (实现 client upsert 时更新路由状态、重构 IN 批量查询为 OR 链、分批提交 context tokens 写入事务，并补全相关单元测试)
- **tests/** — `hub_routing_integration.rs` (+0 / -1) (清理 unused import `MessageQueue` 以满足 Clippy `-D warnings` 要求)
- **docs/exec-plans/completed/todo-store/** — 归档 active exec-plan，写入实施日志与计划说明
- **reviews/m1/** — 记录 M1 评审申请与反馈发现

**合计:** 7 文件, +512 / -28 行 (不含 flowx 配置文件)

## 修复的核心可靠性/安全问题

- **SYNC-02** 活跃路由 vtoken 不同步导致消息路由失败问题 — 在 `upsert_client` 中采用事务，先查询旧 vtoken 再行 upsert。若 vtoken 发生改变，则在同一事务中同步执行 `UPDATE routing_state SET active_vtoken = $new WHERE active_vtoken = $old`，确保活跃路由的 vtoken 状态时刻最新，防止消息被静默丢弃。
- **DB-03** 多数据库元组 `IN` 查询兼容性问题 — 原生的 `(vctx, vtoken) IN (...)` 元组匹配在不同数据库（如 SQLite 或 MySQL 5.7）中支持并不完美。使用 chained `OR` 表达式重构了批量查询的 SQL 构建机制，保证了跨数据库引擎的完全兼容性。
- **DB-02** 大批量广播写锁长占导致阻塞问题 — 在 `persist_context_tokens_batch` 批量持久化时，将原本在单个大事务中写入的所有 tokens 切分为 50 个一组 of chunks。每个 chunk 自主提交事务，极大缩短了单个事务的锁持有时间，防止数据库被持续写入锁死。

## 验证

- `cargo fmt --check` — green
- `cargo clippy --all-targets -- -D warnings` — green
- `cargo test` — green
- `cargo build` — green

## 提交记录 (6 commits)

```
55c462c style: fix unused import in integration test for clippy green
eeb849e style: format test db_url
c45283c docs: archive exec-plan for todo-store
464dfea fix(store): resolve sync-02 concurrent race condition and update review findings
309bc6b feat(store): implement milestone m1 fixes (SYNC-02, DB-03, DB-02)
61938ba style: green the quality-gate baseline (clippy -D warnings + cargo fmt)
```

## 遗留 / 后续

- 无未关闭的 Store 模块可靠性或安全风险
- 准备合并到 `main` 分支前的最终冲突复核
